# AI 에이전트 기본 운영 매뉴얼 (claude.md)

## 1. 기본 소통 규칙 (Communication Rules)
* **언어:** 모든 대답과 설명은 한국어(Korean)로 작성한다.
* **형식:** 결론부터 간결하게 말하며, 불필요한 서론(예: "네, 알겠습니다", "무엇을 도와드릴까요?")은 생략한다.
* **코드 제공:** 코드를 제시할 때는 전체 코드를 무작정 다시 쓰기보다 수정된 부분 위주로 명확히 보여주고, 필요한 경우에만 전체 코드를 작성한다.

## 2. 개발 및 기술 스택 가이드 (Tech Stack & Dev Rules)
* **주요 언어:** Python을 기본으로 사용한다.
* **업무 자동화:** 근무표 생성 등 엑셀 및 데이터 처리 작업 시 `pandas`, `openpyxl` 라이브러리를 적극 활용한다.
* **이미지 처리:** 수도계량기 등 이미지 인식, 객체 분류 작업 시 `OpenCV` 또는 적합한 비전 라이브러리를 활용하며, 처리 속도와 인식 정확도를 고려하여 코드를 최적화한다.
* **코드 스타일:** PEP 8 규약을 준수하며, 주요 함수와 클래스에는 반드시 한국어로 독스트링(Docstring)을 작성하여 나중에 유지보수하기 쉽게 만든다.

## 3. 작업 프로세스 및 권한 (Workflow & Permissions)
* **파일 수정 권한:** 기존 코드를 대폭 수정하거나 삭제하는 등 파괴적인(destructive) 작업을 하기 전에는 반드시 사용자에게 계획을 먼저 설명하고 승인을 받는다.
* **Git 커밋 컨벤션:** 커밋 메시지는 한국어로 명확하게 작성하되, `[feat]`, `[fix]`, `[docs]`, `[refactor]` 등의 태그를 접두어로 사용한다. (예: `[feat] 계량기 이미지 전처리 로직 추가`)
* **패키지 설치:** 새로운 라이브러리나 의존성 설치가 필요할 경우, 터미널 실행 명령어(예: `pip install`)와 설치하는 이유를 명시하여 사용자가 확인할 수 있게 한다.

## 4. 도메인 지식 및 예외 처리 (Domain Context)
* **프로젝트 성격:** 지방상수도 현대화 사업(누수 탐지, 스마트 관망 관리) 및 미터기 교체/인식 등 현장 업무 효율화를 위한 시스템 구축임을 인지하고 작업한다.
* **안정성:** 도메인 특성상 데이터의 누락이나 오류가 치명적일 수 있으므로, 파일 입출력이나 이미지 분석 시 예외 처리(Exception Handling)를 꼼꼼하게 작성한다.
